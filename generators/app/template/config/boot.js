process.env.NODE_ENV = process.env.NODE_ENV || 'development';
process.env.TZID = process.env.TZID || 'UTC';

require('coffee-react-cache');

// other code that needs to run before your app starts goes here.
